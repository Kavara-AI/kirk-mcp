{
  "_comment": "Cursor. Settings > MCP > Add server. Replace k_YOUR_PERSONAL_KEY with the key issued to you. If you hold a service-token pair instead, swap the Authorization header for CF-Access-Client-Id and CF-Access-Client-Secret.",
  "mcpServers": {
    "kirk": {
      "url": "https://kirk-mcp.kavara.ai/mcp",
      "headers": {
        "Authorization": "Bearer k_YOUR_PERSONAL_KEY"
      }
    }
  }
}
